<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PawCare Admin Dashboard</title>
    <link rel="stylesheet" href="assets/css/style.css">
    <style>
        /* CSS Tambahan Khusus untuk Halaman CRUD (Master & Transaksi) */
        .crud-table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        .crud-table th, .crud-table td { padding: 15px; border-bottom: 1px solid #ecf0f1; text-align: left; }
        .crud-table th { background-color: #fdf6f0; color: #e67e22; font-weight: 600; }
        .crud-table tr:hover { background-color: #f9f9f9; }
        
        .card { background: #fff; padding: 25px; border-radius: 12px; box-shadow: 0 4px 15px rgba(0,0,0,0.03); margin-bottom: 20px; }
        
        .form-group { margin-bottom: 15px; }
        .form-group label { display: block; margin-bottom: 8px; font-weight: 500; color: #2c3e50; }
        .form-control { width: 100%; padding: 10px 15px; border: 1px solid #ddd; border-radius: 6px; outline: none; transition: 0.3s; }
        .form-control:focus { border-color: #e67e22; box-shadow: 0 0 5px rgba(230, 126, 34, 0.3); }
        
        .btn-sm { padding: 8px 12px; font-size: 13px; }
        .btn-warning { background-color: #f1c40f; color: #fff; }
        .btn-danger { background-color: #e74c3c; color: #fff; }
        .btn-secondary { background-color: #95a5a6; color: #fff; }
    </style>
</head>
<body></body>